from setuptools import setup, find_packages

setup(
    name="verl_spectrum_patch",
    version="0.1.0",
    packages=find_packages(),
    description="A patch for verl to enable Spectrum parameter freezing via sitecustomize.",
) 